apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: redisinsight-allow-ips
  namespace: istio-system
spec:
  selector:
    matchLabels:
      istio: ingressgateway
  action: ALLOW
  rules:
    - to:
        - operation:
            hosts: ["redis.dpsrv.me"]
      from:
        - source:
            ipBlocks: ["96.239.100.73/32"]
